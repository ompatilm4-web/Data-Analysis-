# 🧠 NumPy & Pandas Hard Practice Questions

## 🚀 Goal
These 40 problems are designed to make you strong in:
- NumPy array manipulation
- Broadcasting & vectorization
- Linear algebra for ML
- Pandas data cleaning
- GroupBy, merging, and advanced operations

---

# 🔹 NUMPY (20 Hard Questions)

## 🧩 Array Logic & Manipulation

1. Create a 5x5 matrix with values 1–25 and replace the border elements with 0.
2. Reverse a NumPy array without using slicing (`[::-1]`).
3. Create a 10x10 matrix and extract the 3x3 center block.
4. Swap two rows of a matrix without using a temporary variable.
5. Replace all even numbers in an array with -1 without using loops.
6. Find indices of top 3 maximum values in a 1D array.
7. Create a checkerboard pattern (8x8) using NumPy only.
8. Normalize an array between 0 and 1.
9. Find the most frequent value in an array.
10. Remove duplicate rows from a 2D array.

---

## 📌 Broadcasting & Advanced Indexing

11. Add a 1D array to every row of a 2D array using broadcasting.
12. Subtract column-wise mean from a matrix.
13. Create a matrix where element (i,j) = i + j.
14. Multiply each column by different scalar values.
15. Filter rows where column 2 > column 1.

---

## 📐 Linear Algebra & ML-Based

16. Compute eigenvalues and eigenvectors of a matrix.
17. Manually implement matrix multiplication using NumPy (no `@` or `dot`).
18. Compute cosine similarity between two vectors.
19. Compute covariance matrix of dataset.
20. Implement mini PCA (without sklearn).

---

# 📊 PANDAS (20 Hard Questions)

## 🧹 Data Cleaning

21. Replace missing values in a column with median grouped by category.
22. Detect and remove outliers using IQR method.
23. Find duplicate rows based on two columns only.
24. Convert string date column to datetime and extract year, month, day.
25. Replace values conditionally (if salary < 20000 → mark as “Low”).

---

## 📊 GroupBy & Aggregation

26. Find top 3 highest salary employees in each department.
27. Calculate percentage contribution of each category.
28. Find departments where average salary > overall average.
29. Count unique values per group.
30. Multi-index grouping with two columns and aggregate mean & sum.

---

## 🔗 Merging & Joining

31. Perform inner, left, right join between two DataFrames.
32. Find rows present in df1 but not in df2.
33. Merge 3 datasets on common column.

---

## ⚙ Advanced Pandas

34. Pivot table with multiple aggregation functions.
35. Unstack and restack a multi-index DataFrame.
36. Apply custom function to each row.
37. Create rolling average column (window size 3).
38. Rank employees within department.
39. Create new column based on multiple conditions.
40. Optimize memory usage of large DataFrame.

---

# 🏆 If You Complete All 40

You will be:
- Strong in NumPy fundamentals
- Confident with Pandas real-world tasks
- Ready for Data Science interviews
- Comfortable handling ML datasets

---

💡 Suggested Strategy:
- Solve 5 questions daily
- Avoid loops in NumPy
- Use vectorization
- Practice with real datasets

Good luck 🚀
